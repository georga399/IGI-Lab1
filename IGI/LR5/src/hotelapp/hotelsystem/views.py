from django.shortcuts import render
from .models import Reservation


# Create your views here.
def client_home(request):
    pass
    # reservations = Reservation.objects.filter(user=)

def staff_home(request):
    pass
